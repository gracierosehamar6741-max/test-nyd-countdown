<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>New Year – All Timezones Countdown</title>

  <!-- Luxon for reliable timezone math -->
  <script src="https://cdn.jsdelivr.net/npm/luxon@3/build/global/luxon.min.js"></script>

  <style>
    :root {
      --bg: #050608;
      --panel: rgba(255,255,255,.06);
      --text: rgba(255,255,255,.92);
      --muted: rgba(255,255,255,.65);
      --accent: rgba(120, 190, 255, .9);
      --danger: rgba(255, 120, 120, .9);
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      background: radial-gradient(1200px 700px at 50% 0%, rgba(120,190,255,.14), transparent 60%),
                  radial-gradient(900px 600px at 90% 30%, rgba(255,120,120,.10), transparent 55%),
                  var(--bg);
      color: var(--text);
      overflow: hidden;
    }

    /* Main layout */
    .wrap { position: relative; width: 100vw; height: 100vh; display: grid; place-items: center; padding: 24px; }
    .grid {
      width: min(1100px, 100%);
      display: grid;
      grid-template-columns: 1.35fr .65fr;
      gap: 18px;
      align-items: stretch;
    }
    @media (max-width: 900px) { .grid { grid-template-columns: 1fr; } }

    .card {
      background: var(--panel);
      border: 1px solid rgba(255,255,255,.08);
      border-radius: 18px;
      box-shadow: 0 18px 60px rgba(0,0,0,.45);
      backdrop-filter: blur(10px);
      overflow: hidden;
    }

    .hero { padding: 22px 22px 18px; }
    .titleRow { display:flex; align-items: baseline; justify-content: space-between; gap: 14px; flex-wrap: wrap; }
    h1 { margin: 0; font-size: 18px; letter-spacing: .08em; text-transform: uppercase; color: var(--muted); }
    .status { font-size: 13px; color: var(--muted); }
    .status b { color: var(--text); }

    .bigTime {
      margin-top: 14px;
      font-variant-numeric: tabular-nums;
      font-size: clamp(46px, 6.2vw, 78px);
      letter-spacing: .04em;
      line-height: 1.05;
      text-shadow: 0 0 22px rgba(120,190,255,.18);
    }
    .sub {
      margin-top: 10px;
      color: var(--muted);
      font-size: 14px;
      display:flex; gap: 10px; flex-wrap: wrap; align-items: center;
    }
    .pill {
      padding: 6px 10px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,.10);
      background: rgba(0,0,0,.18);
      font-size: 12px;
      color: var(--muted);
    }
    .pill strong { color: var(--text); font-weight: 600; }

    .side { padding: 16px; display:flex; flex-direction: column; gap: 12px; }
    .
